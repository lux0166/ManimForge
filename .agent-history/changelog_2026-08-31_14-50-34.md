# Technical Changelog — 2026-08-31 14:50:34

## 1. Summary of Changes
- **Proximity Sidebar Integration**: Integrated `@beui` Proximity Sidebar as a magnetic timeline minimap for chat turns with spring physics (`useSpring`) and smooth navigation.
- **Component Suite Unpacking**: Installed and verified full beUI ecosystem components:
  - `@beui/tool-result`
  - `@beui/agent-activity`
  - `@beui/message`
  - `@beui/loader`
  - `@beui/shader-background` (`@paper-design/shaders-react`)
  - `@beui/animated-badge`
  - `@beui/chat-app`
  - `@beui/combobox`
- **UI Bloat Cleanup**: Removed unnecessary bottom status indicators (`Python 3.14 • Manim Engine`) in sidebar for maximum vertical cleanliness.
- **PromptBar Evolution**:
  - Replaced legacy textareas with the official, ultra-sleek `PromptBar` supporting WebGL `glimm` Rainbow Sweep shader on model transition.
  - Implemented `@` Data Source lookup and `/` Slash Command menu with gliding highlight physics (`bg-hover`).
  - Fixed popup menu clipping by removing `overflow-hidden` from bottom container and raising menu z-index to `z-50 backdrop-blur-md`.
  - Added seamless `Tab` key shortcut for switching between Direct Execution and Plan Mode with zero layout distortion (`agy • Plan ▾`).
- **Text Wrap & Scrollbar Fixes**:
  - Implemented `break-all [overflow-wrap:anywhere]` across all user bubbles and agent responses to prevent unbroken long strings from causing layout overflow.
  - Locked horizontal overflow (`overflow-x-hidden`) to eliminate unsightly horizontal scrollbars.

## 2. Verification
- `npx tsc --noEmit`: 0 errors
- `npm run build`: 0 errors (2,341 modules transformed)
- Live server: Active on `http://localhost:1420/`
