# Technical Changelog — 2026-08-31 17:08:13

## 1. Summary of Changes
- **Project Isolation & Clean "+ New Video" Handler (`src/App.tsx`)**:
  - Fixed issue where creating a new video or switching projects kept old chat messages, code, and black video preview.
  - Implemented `handleNewVideo`: Instantly resets `videoUrl` to `null`, `renderStatus` to `idle`, `renderProgress` to 0, loads clean template code, and initializes a fresh welcome message.
  - Implemented `handleSelectProject`: Asynchronously loads the selected project's `scene.py`, `chat.json` conversation history, and latest `.mp4` video from disk.
- **Backend Persistent State (`src-tauri/src/project_manager.rs` & `lib.rs`)**:
  - Added `save_chat` and `load_chat` Tauri IPC commands for per-project conversation persistence.
  - Added `get_project_video` command to scan and return the latest rendered `.mp4` video.
  - Fixed Rust raw string escaping and exported `detect_environment` & `detect_available_agents`.

## 2. Verification
- `cargo check`: Finished `dev` profile target in 0.77s (0 errors).
- `npm run build`: 2,336 modules transformed (0 errors).
- GitHub Repo: Pushed commit `15a6308` to `main` branch.
- Desktop App: Running live on `manimforge.exe`.
