# Technical Changelog — 2026-08-31 18:42:30

## 1. Summary of Changes
- **Vietnamese Typography & Diacritic Glyph Preservation (`backend/server.py`)**:
  - Resolved missing-glyph white rectangle ("tofu" box) and detached accent bugs (`ề`, `ậ`, `ắ`, `ỗ`, `ự`) in Manim Cairo rendering on Windows.
  - Implemented `patch_vietnamese_fonts(code)` AST pre-processor in `backend/server.py` to auto-inject `font="Segoe UI"` into all non-ASCII `Text(...)` calls.
  - Enforced `font="Segoe UI"` and `font="Arial"` standards across AI system prompts.
- **Strict Visual Anti-Collision & Axis Spacing Rules**:
  - Eliminated horizontal axis number smearing caused by small fractional steps in `x_range`/`y_range` with unrounded coordinates.
  - Standardized clean integer steps and discrete landmark labeling (`x_range=[-2*PI, 2*PI, PI/2]` with spaced $\pi$ labels).
  - Protected apex labels from colliding with top coordinate numbers using `to_corner(UR, buff=0.8)`.
- **Elimination of `extbf` LaTeX Escaping Artifacts**:
  - Eradicated ASCII Tab (`\t`) escaping collisions in Python strings by using native Manim bold formatting (`Text("...", weight=BOLD, font_size=28)`).
- **Deep Learning Neural Network Visual Architecture**:
  - Upgraded AI code synthesis to construct complete multilayer topologies (Input, Hidden, Output layers with synaptic weight lines).
  - Integrated forward pass light-pulse activations and backward pass backpropagation gradient animations.
- **Dynamic State Restoration & Timeout Safeguards**:
  - Added `GET /api/project_video` endpoint and connected with `fetchProjectVideo` to immediately restore rendered video on project select/mount.
  - Added strict `timeout=60` and `timeout=120` guards to all FFmpeg and Manim subprocess executions.

## 2. Empirical Verification
- **Vite Build**: Compiled 2,333 modules successfully with 0 TypeScript/build errors.
- **Backend API**: Verified `GET /api/health`, `GET /api/projects`, `GET /api/project_video`, `POST /api/chat`, and `POST /api/merge_scenes` on `http://127.0.0.1:8765`.
- **Runtime Manim Execution**: Generated and rendered neural network and trigonometric animations in 60fps with zero missing glyphs and zero text collisions.
